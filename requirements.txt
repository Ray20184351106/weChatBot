# WeChatHook (微信自动化)
# 支持微信版本：3.9.5.81 ~ 4.1.1
wxhook>=0.0.1

# LLM 相关 - 使用宽松版本让 pip 自动解决冲突
torch>=2.0.0
transformers>=4.30.0,<4.50.0  # 避免与 regex 冲突
peft>=0.5.0
accelerate>=0.20.0,<1.0.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0
datasets>=2.14.0,<4.0.0  # 避免与 huggingface_hub 冲突

# 工具库
pyyaml>=6.0
python-dotenv>=1.0.0
loguru>=0.7.0
httpx>=0.25.0  # API 调用

# 测试
pytest>=7.4.0
pytest-asyncio>=0.21.0

# 代码质量
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
